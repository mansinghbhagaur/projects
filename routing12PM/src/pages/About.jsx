import React from 'react'
import { useParams } from 'react-router';

const About = () => {
      // const { id } = useParams();
      // console.log(id, "hello")
      return (
            <div>
                  <h1>ABout PAge </h1>
            </div>
      )
}

export default About
